name: Deploy Demo

on:
  push:
      branches:
        - master
  workflow_dispatch:

jobs:
  deploy_demo:
    name: Deploy Demo Application to Github Pages

    strategy:
      matrix:
        os: [ubuntu-latest]
        bun: [1.3.14]

    runs-on: ${{ matrix.os }}

    permissions:
      contents: read
      pages: write
      id-token: write

    concurrency:
      group: pages
      cancel-in-progress: true

    steps:
      - uses: actions/checkout@v4
      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ matrix.bun }}
      - name: Setup Pages
        uses: actions/configure-pages@v3
      - name: Install Dependencies
        run: bun i --frozen-lockfile
      - name: Build Resources
        run: bun run build
      - name: Upload Static Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'test/preview'
      - name: Deploy Site to Github Pages
        id: deploy
        uses: actions/deploy-pages@v4